"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.parseSingleTestResults = exports.parseSuites = void 0;
const parseTests_1 = __importDefault(require("./parseTests"));
function parseSuites(foundCases, context) {
    var _a, _b;
    if (((_a = context.suites) === null || _a === void 0 ? void 0 : _a.length) > 0) {
        // eslint-disable-next-line no-param-reassign
        context.suites.reduce(parseSuites, foundCases);
        // foundCases = parseSuites(context.suites, foundCases)
    }
    if (((_b = context.tests) === null || _b === void 0 ? void 0 : _b.length) > 0) {
        // eslint-disable-next-line no-param-reassign
        foundCases = parseTests_1.default(context.tests, foundCases);
        // foundCases = parseTests(context.tests, foundCases)
    }
    return foundCases;
}
exports.parseSuites = parseSuites;
/**
 * Parse results of a single test file (example.spec.js)
 * @param acc
 * @param result
 */
function parseSingleTestResults(acc, result) {
    const testResult = {
        file: result.file,
        cases: {},
    };
    parseSuites(testResult.cases, result);
    // result.suites.reduce(parseSuite, testResult.cases)
    // Store
    acc.push(testResult);
    return acc;
}
exports.parseSingleTestResults = parseSingleTestResults;
